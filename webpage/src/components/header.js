import styles from '../styles/header.module.css';

export const Header = () => {
    return (
        <header className={`${styles.header}`}>
            {/*<div className={`${styles.left}`}>*/}
            {/*    <h2>home</h2>*/}
            {/*    <h2>about</h2>*/}
            {/*</div>*/}
        </header>
    );
};