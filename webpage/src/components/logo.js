import styles from '../styles/logo.module.css';

export const Logo = () => {
    return (
        <div className={`${styles.logo}`}>
            <h1>Fred</h1>
            <h1>Davison</h1>
        </div>
    );
}